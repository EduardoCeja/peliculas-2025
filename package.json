{
  "name": "movie-catalog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "chmod +x node_modules/.bin/serve",
    "start":       "serve -s . -l $PORT"
  },
  "dependencies": {
    "firebase": "^11.7.1",
    "serve":   "^14.0.1"
  }
}